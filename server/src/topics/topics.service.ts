import { Injectable } from '@nestjs/common';

/**
 * Service layer for the Topics feature module.
 *
 * Provides data access methods for `Topic` records.
 *
 * @todo Implement Prisma-backed CRUD methods:
 *   - `findAll()` — return all topics
 *   - `findOne(id)` — return a single topic by ID
 *   - `findBySlug(slug)` — return a topic by slug
 */
@Injectable()
export class TopicsService {}
