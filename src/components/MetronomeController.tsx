// function MetronomeController() {
//    const { tick, ready } = useMetronome();

//    useEffect(() => {
//       if (ready) {
//          tick(true); // test accent click
//       }
//    }, [ready]);

//    return <div>Metronome ready: {ready ? "yes" : "no"}</div>;
// }
